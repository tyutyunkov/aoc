const parse = input => input

exports.part1 = input => {
    const rows = parse(input);
    // Do something with rows for part 1

    return 0;
};

exports.part2 = input => {
    const rows = parse(input);
    // Do something with rows for part 2

    return 0;
};